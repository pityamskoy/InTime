stages:
  - test
  - build
  - deploy

test-job:
  stage: test
  image: maven:4.0.0-rc-5-amazoncorretto-25
  script:
    - mvn test

build-job:
  stage: build
  script:
  - docker build --network=host -t my-backend .

deploy-job:
  stage: deploy
  script:
    - docker compose up -d
