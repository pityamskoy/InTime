stages:
  - build
  - test
  - deploy

test-job:
  stage: test
  script:
    - mvn test

build-job:
  stage: build
  script:
  - docker -t my-backend .

deploy-job:
  stage: deploy
  script:
    - docker compose up -d
